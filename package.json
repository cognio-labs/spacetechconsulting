{
  "scripts": {
    "build": "npm --prefix spacetechconsulting-main run build && rm -rf .vercel/output && mkdir -p .vercel && cp -R spacetechconsulting-main/.vercel/output .vercel/output"
  },
  "private": true
}
